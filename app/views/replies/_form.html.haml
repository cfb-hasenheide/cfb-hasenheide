= bootstrap_form_for reply do |f|
  = f.hidden_field :user_id, value: reply.user_id
  = f.hidden_field :event_id, value: reply.event_id

  = f.select :status,
    status_options,
    onchange: 'this.form.submit();'

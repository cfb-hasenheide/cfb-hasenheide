= content_for :title, 'Spieler'

.page-header
  %h1
    = content_for :title
    - if admin?
      .pull-right
        = link_to for_user_new_player_path, class: 'primary-button' do
          = create_icon
          %span.hidden-xs Spieler erstellen

- @players.in_groups_of(4) do |players|
  .row
    - players.compact.each do |player|
      .col-xs-12.col-sm-6.col-md-4.col-lg-3
        = render player

= render 'layouts/pagination', entries: @players

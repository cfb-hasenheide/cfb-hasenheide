= content_for(:title, "Meldeliste #{@event.name}")

= render 'events/subnavigation', event: @event

.page-header
  %h1
    Meldeliste
    %small= @event.name

= render 'events/basic_information', event: @event

= @attendees_list.open?
= @attendees_list.minimum
= @attendees_list.maximum

%ul.list-group
  - news_by_month.each do |beginning_of_month, news|
    = link_to "##{l(beginning_of_month, format: :month_and_year)}",
      class: 'list-group-item list-group-item-secondary list-group-item-action d-flex justify-content-between align-items-center',
      'data-toggle': 'collapse',
      role: 'button',
      'aria-expanded': 'false',
      'aria-controls': l(beginning_of_month, format: :month_and_year) do
      = l(beginning_of_month, format: :month_and_year)
      .badge.badge-dark.badge-pill
        = news.count

    .collapse{id: l(beginning_of_month, format: :month_and_year)}
      - news.each do |news|
        = link_to news.title,
          news_path(news.id),
          class: 'list-group-item list-group-item-action'

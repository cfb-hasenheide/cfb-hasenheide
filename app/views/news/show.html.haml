.page-header
  %h1
    = @news.title
    %small.pull-right= l(@news.created_at, format: :date_only)

.panel{class: (@news.internal? ? 'panel-default' : 'panel-primary')}
  .panel-body
    = @news.content.html_safe

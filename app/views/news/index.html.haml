.page-header
  %h1
    News
    %small= page_entries_info @news

    - if admin?
      .pull-right
        = link_to new_news_path, class: 'primary-button' do
          = create_icon
          News erstellen

- for news in @news
  = render 'news', news: news

.pull-right= paginate(@news)

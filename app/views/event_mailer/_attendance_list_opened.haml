%p Hallo Hasen,

%p auf der Page werden jetzt Meldungen für folgendes Event entgegen genommen:

%p
  %strong= event.name
  = surround '(', ')' do
    = event.model_name.human

%p
  Wann:
  = l(event.datetime)

%p
  Wo:
  = event.address

%p= event.description.presence

%p= message.presence

= link_to 'Melden!', event_url(event)

$('#messages').html("<%= escape_javascript(render @messages) %>");
<%# FIXME: scroll down to last message
// $('#messages').closest('.modal-body').scrollTop($('#messages').closest('.modal-body')[0].scrollHeight);

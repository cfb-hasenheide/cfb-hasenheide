$('.chatMessages').html("<%= escape_javascript(render @messages) %>");
$('.chatMessages').closest('.chatMessagesContainer').scrollTop($('.chatMessages').closest('.chatMessagesContainer').get(0).scrollHeight);

= content_for :title, "#{@event.name} #{t('edit_model_name', model_name: Event.model_name.human)}"

.card
  .card-header
    = render 'events/header_with_subnavigation',
      event: @event,
      subtitle: t('edit_model_name', model_name: Event.model_name.human)

  .card-body
    = render "events/#{@event.class.to_s.underscore.pluralize}/form", event: @event

.page-header
  %h1
    Spielberichte
    %small= page_entries_info(@reports)

    - if admin?
      .pull-right
        = link_to new_report_path, class: 'primary-button' do
          = create_icon
          Bericht erstellen

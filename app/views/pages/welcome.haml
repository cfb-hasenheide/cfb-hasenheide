.page-header
  %h1 Willkommen!

.panel.panel-primary
  .panel-body
    Hier entsteht eine neue Internetpräsenz des CFB Hasenheide. Bitte folgen
    Sie diesem Link, um auf die aktuelle Seite zu gelangen:
    = link_to 'http://www.cfb-hasenheide.de', 'http://www.cfb-hasenheide.de'

.row
  .col-md-9
    - @news.each do |news|
      = render 'news/news', news: news

  .col-md-3
    .panel.panel-primary
      .panel-heading
        %h3.panel-title Events
      .panel-body
        Kommende
      .list-group
        - @upcoming_events.each do |upcoming_event|
          .list-group-item
            = upcoming_event.generated_name
      .panel-body
        Vergangene
      .list-group
        - @past_events.each do |past_event|
          = link_to event_path(past_event), class: 'list-group-item' do
            %h5.list-group-item-heading
              = past_event.generated_name
              - if past_event.past? && past_event.report.present?
                %span.pull-right.label{class: event_label_css_class(past_event)}
                  = past_event.report.final_score
            %p.list-group-item-text
              = date_icon
              = l(past_event.datetime, format: :date_only)


.halfs
  .half
    .panel.panel-primary
      .panel-heading
        %h3.panel-title CFB Hasenheide I - Ü32 Freizeit Landesliga KF
      .panel-body
        #cfbIFussballDeWidget
          = spinner_icon
          Lade Tabelle

  .half
    .panel.panel-primary
      .panel-heading
        %h3.panel-title CFB Hasenheide II - Herren Freizeit Landesliga St.1 KF
      .panel-body
        #cfbIIFussballDeWidget
          = spinner_icon
          Lade Tabelle

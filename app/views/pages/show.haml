= content_for :title, @page.header

.page-header
  %h1
    = content_for :title
    %small
      = @page.subheader.presence

= @markdown.render(@page.content).html_safe

- if can?(:update, Page)
  = link_to edit_page_path(@page), class: 'default-button' do
    = edit_icon
    Bearbeiten
